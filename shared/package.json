{
  "name": "@spec-flow/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Tipos compartilhados entre client e server (contrato de exibição). Exporta TS source — Vite e tsx transpilam na hora, sem build.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
